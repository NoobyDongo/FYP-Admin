const base = "/admin";
const link={
    base,
    business: base + "/business",
    inventory: base + "/inventory",
    signin: base + "/signin",
    product: base + "/product",
    demo: base + "/demo",
    settings: base + "/settings",
}
export default link